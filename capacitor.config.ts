import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bibliophilia.android',
  appName: 'bibliophilia',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
